apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-primary
  namespace: argocd
  labels:
    app.kubernetes.io/name: vault
    app.kubernetes.io/part-of: gitops-control-plane
    platform.hashicorp.com/environment: primary
    platform.hashicorp.com/phase: "3"
spec:
  project: platform
  source:
    repoURL: https://github.com/Leninfitfreak/Vault.git
    targetRevision: main
    path: platform/vault
    helm:
      releaseName: vault
      valueFiles:
        - values/primary.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: vault
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ServerSideApply=true

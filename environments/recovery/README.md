# Recovery Environment

The recovery environment represents the secondary cluster target.

Recovery values render successfully and are reconciled by Argo CD when the recovery Application is active.

Workloads remain inactive because failover, restore, and traffic switching are later phases.

{{- if .Values.global.active }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.orders.name }}
  namespace: {{ include "orders-service.namespace" . }}
  labels:
{{ include "orders-service.componentLabels" (dict "Values" .Values "Release" .Release "Chart" .Chart "component" "orders") | indent 4 }}
spec:
  replicas: {{ .Values.orders.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: orders
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: orders
        app.kubernetes.io/name: {{ .Values.orders.name }}
    spec:
      serviceAccountName: {{ .Values.orders.name }}
      securityContext:
{{ include "orders-service.podSecurityContext" . | indent 8 }}
      containers:
        - name: {{ .Values.orders.name }}
          image: "{{ .Values.images.application.repository }}:{{ .Values.images.application.tag }}"
          imagePullPolicy: {{ .Values.images.application.pullPolicy }}
          command: ["python", "/app/server.py"]
          ports:
            - name: http
              containerPort: {{ .Values.orders.port }}
{{- if .Values.orders.mtls.enabled }}
            - name: https-mtls
              containerPort: {{ .Values.orders.mtls.port }}
{{- end }}
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.vaultBacked.destinationName }}
                  key: API_KEY
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
{{- if .Values.secrets.dynamicDatabase.enabled }}
                  name: {{ .Values.secrets.dynamicDatabase.destinationName }}
                  key: DB_USERNAME
{{- else }}
                  name: {{ .Values.secrets.ordersCredentials.name }}
                  key: DB_USERNAME
{{- end }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
{{- if .Values.secrets.dynamicDatabase.enabled }}
                  name: {{ .Values.secrets.dynamicDatabase.destinationName }}
                  key: DB_PASSWORD
{{- else }}
                  name: {{ .Values.secrets.ordersCredentials.name }}
                  key: DB_PASSWORD
{{- end }}
            - name: POSTGRESQL_HOST
              value: {{ .Values.postgresql.name }}
            - name: POSTGRESQL_PORT
              value: {{ .Values.postgresql.port | quote }}
            - name: MTLS_ENABLED
              value: {{ .Values.orders.mtls.enabled | quote }}
{{- if .Values.orders.mtls.enabled }}
            - name: MTLS_PORT
              value: {{ .Values.orders.mtls.port | quote }}
            - name: MTLS_CA_FILE
              value: {{ .Values.orders.mtls.caFile | quote }}
            - name: MTLS_SERVER_CERT_FILE
              value: {{ .Values.orders.mtls.certFile | quote }}
            - name: MTLS_SERVER_KEY_FILE
              value: {{ .Values.orders.mtls.keyFile | quote }}
{{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
{{ toYaml .Values.orders.resources | indent 12 }}
          securityContext:
{{ include "orders-service.securityContext" . | indent 12 }}
          volumeMounts:
            - name: app
              mountPath: /app
            - name: tmp
              mountPath: /tmp
{{- if .Values.orders.mtls.enabled }}
            - name: mtls-server
              mountPath: {{ .Values.orders.mtls.certMountPath }}
              readOnly: true
{{- end }}
      volumes:
        - name: app
          configMap:
            name: {{ .Values.orders.name }}-app
        - name: tmp
          emptyDir: {}
{{- if .Values.orders.mtls.enabled }}
        - name: mtls-server
          secret:
            secretName: {{ .Values.secrets.pki.serverDestinationName }}
{{- end }}
{{- end }}
